// TODO make the port configurable. Take the env variable from docker-compose
// TODO make host configurable
export const environment = {
  production: true,
  baseURL: 'http://localhost:6969/api',
};
